import { Link, useLocation } from 'react-router-dom';
import { Clock3, RotateCcw } from 'lucide-react';
export function SessionExpiredPage(){const location=useLocation();const returnTo=(location.state as {returnTo?:string}|null)?.returnTo??'/';return <div className="vc-state-page vc-page-width"><section className="vc-state-card"><div className="vc-state-card__icon"><Clock3 size={42}/></div><span className="vc-state-card__eyebrow">Secure session</span><h1>Your session has expired</h1><p>Sign in again to continue. Your VoiceCloud account and server-side data remain unchanged.</p><div className="vc-state-card__notice"><RotateCcw size={18}/><span>After sign-in, VoiceCloud can return you to the page you were trying to open.</span></div><div className="vc-state-card__actions"><Link className="vc-button vc-button--primary" to="/auth/sign-in" state={{returnTo}}>Sign In Again</Link><Link className="vc-button vc-button--secondary" to="/">Return Home</Link></div></section></div>}
