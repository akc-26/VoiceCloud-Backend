import { Link, useLocation } from 'react-router-dom';
import { LockKeyhole, ShieldAlert } from 'lucide-react';
export function RestrictedAccountPage(){const location=useLocation();const message=(location.state as {message?:string}|null)?.message;return <div className="vc-state-page vc-page-width"><section className="vc-state-card"><div className="vc-state-card__icon"><ShieldAlert size={42}/></div><span className="vc-state-card__eyebrow">Account access</span><h1>Access is temporarily restricted</h1><p>{message??'VoiceCloud could not authorize this account for the requested action. Follow the account instructions shown by VoiceCloud or try again later.'}</p><div className="vc-state-card__notice"><LockKeyhole size={18}/><span>This state reflects backend authorization. The website does not bypass account restrictions.</span></div><div className="vc-state-card__actions"><Link className="vc-button vc-button--primary" to="/auth/sign-in">Return to Sign-In</Link><Link className="vc-button vc-button--secondary" to="/">Go Home</Link></div></section></div>}
